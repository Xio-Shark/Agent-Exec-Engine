apiVersion: v1
kind: Service
metadata:
  name: agent-exec-engine
  labels:
    app: agent-exec-engine
spec:
  type: ClusterIP
  selector:
    app: agent-exec-engine
  ports:
    - name: http
      port: 8080
      targetPort: http
